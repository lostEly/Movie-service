spring:
  datasource:
    url: jdbc:mysql://localhost:3306/movie
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      format_sql: true

  liquibase:
    enabled: true
    change-log: changelog-master.xml
